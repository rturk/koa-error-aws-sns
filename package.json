{
  "name": "koa-error-aws-sns",
  "version": "0.0.2",
  "description": "Koa - Send server errors to an AWS SNS topic - ie notifyMe",
  "main": "lib/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "prepublish": "npm run build",
    "build": "rm -rf lib/* && babel src --ignore *.spec.js --out-dir lib"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/rturk/koa-error-sns.git"
  },
  "author": "",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/rturk/koa-error-aws-sns/issues"
  },
  "homepage": "https://github.com/rturk/koa-error-aws-sns#readme",
  "dependencies": {
  },
  "peerDependencies": {
    "aws-sdk": "^2.3.2"
  },
  "devDependencies": {
    "babel-cli": "^6.6.5",
    "babel-preset-es2015": "^6.6.0",
    "babel-preset-stage-0": "^6.5.0"
  }
}
